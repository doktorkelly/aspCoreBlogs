@model AspCore1.Model.Blog
@{
    ViewData["Title"] = "Detail";
}

<!--
<h1>@ViewData["Title"]</h1>

<div class="container">
    <div class="row">
        <div class="col-xs-4 col-sm-2">@Html.Label("Title")</div>
        <div class="col-xs-8 col-sm-6">@Html.DisplayFor(item => Model.Title)</div>
    </div>
    <div class="row">
        <div class="col-xs-4 col-sm-2">@Html.Label("Date")</div>
        <div class="col-xs-8 col-sm-6">@Html.DisplayFor(mod => mod.CreatedAt)</div>
    </div>
    <div class="row">
        <div class="col-xs-4 col-sm-2">@Html.Label("Text", "Content")</div>
        <div class="col-xs-4 col-sm-6">@Html.DisplayFor(mod => mod.Text)</div>
        @*<div class="col-xs-4 col-sm-6">@Html.Display("Text", "Content")</div>*@
        @*<div class="col-xs-8 col-sm-6">@Html.TextAreaFor(mod => mod.Text, new { @rows=10, @cols=50 })</div>*@
    </div>
</div>
-->

<div container>
    <div class="blog-header">
        <h1 class="blog-title">@ViewData["Title"]</h1>
        <p class="lead blog-description">The official Blog</p>
        <a asp-controller="Blog1" asp-action="Index">Back to List</a>
    </div>

    <div class="row">
        <div class="col-sm-8 blog-main">
            <div class="blog-post">
                <h2 class="blog-post-title">@Model.Title</h2>
                <p class="blog-post-meta">@Model.CreatedAt by <a href="#">AUTHOR</a></p>
                <p>@Model.Text</p>
            </div>
        </div>
    </div>
</div>
